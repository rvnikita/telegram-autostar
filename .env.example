# Get these from https://my.telegram.org
API_ID=your_api_id_here
API_HASH=your_api_hash_here

# Optional: limit to specific chats (comma-separated chat IDs or usernames)
# Leave empty to track all chats
WATCH_CHATS=

# Reaction to add (default: star)
REACTION=⭐

# Delay between reactions in seconds (to avoid rate limits)
REACTION_DELAY=1.0
